# Agent Web Enricher Job Definition
# Demonstrates web enrichment using agents to add external information to documents

id = "agent-web-enricher"
name = "Agent Web Enricher"
type = "agent"
job_type = "user"
description = "Enriches documents with web-sourced information using Google Gemini ADK"
tags = ["agent", "enrichment", "web"]
schedule = ""
timeout = "15m"
enabled = false
auto_start = false

[step.enrich_with_web_content]
type = "agent"
description = "Enrich documents with web-sourced information via AI analysis"
on_error = "continue"
operation_type = "enrich"
agent_type = "web_enricher"
api_key = "{google_search_api_key}"
filter_tags = ["needs-enrichment"]
filter_limit = 50
filter_created_after = "2025-01-01T00:00:00Z"
