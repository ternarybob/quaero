Compressively review the html pages. Specifically, docused on the CSS and styling. 
1. Preference default and basic styles from https://spectre-org.github.io/spectre-docs/docs/, whilst preserving the quaero.css styles where applicable. 
2. Preference default and basic styles, rather than element specific styles.
3. Remove any redundant CSS and HTML. 
4. Where possible and where the javascvript is used in multiple pages, create a lib and import as needed. Similar to  pages\static\websocket-manager.js