# GitHub Repository Content Collector (By Name)
# Tests connector_name configuration instead of connector_id
# Documents include metadata: owner, repo, branch, folder, path, sha, file_type

id = "github-repo-collector-by-name"
name = "GitHub Repository Collector (By Name)"
type = "fetch"
job_type = "user"
source_type = "github"
description = "Fetches repository content using connector_name instead of connector_id"
tags = ["github", "source-code", "repository", "by-name-test"]
schedule = ""
timeout = "30m"
enabled = true
auto_start = false

[step.fetch_repo_content]
action = "github_repo_fetch"
on_error = "continue"
connector_name = "Test GitHub Connector"
owner = "ternarybob"
repo = "quaero"
branches = ["main"]
extensions = [".go", ".ts", ".tsx", ".js", ".jsx", ".md", ".yaml", ".yml", ".toml", ".json"]
exclude_paths = ["vendor/", "node_modules/", ".git/", "dist/", "build/"]
max_files = 10

[error_tolerance]
max_child_failures = 50
failure_action = "continue"
