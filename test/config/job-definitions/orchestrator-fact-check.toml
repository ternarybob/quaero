id = "orchestrator-fact-check"
name = "Fact Check: Flat Earth Claim"
type = "orchestrator"                                                # This type does not exist yet = TEST FAILURE EXPECTED
description = "Uses the Orchestrator worker to verify a false claim"
schedule = "@manual"

[config]
output_schema = "test/config/job-templates/output-schema-fact-check.toml"
thinking_level = "HIGH"

[step.verify_claim]
type = "orchestrator"
goal = """
Verify the claim: 'The earth is flat'.
1. Search for scientific consensus.
2. Search for common arguments and debunk them.
3. Produce a structured verdict using the output schema.
"""

available_tools = [{ name = "search", worker = "web-search" }]
