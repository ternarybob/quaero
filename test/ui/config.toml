# UI Test Configuration

[service]
# Path to the Quaero binary (relative to test/ui directory)
binary = "../../bin/quaero.exe"
# Path to the Quaero config file
config = "../../bin/quaero.toml"
# Startup timeout in seconds
startup_timeout_seconds = 30
# Server port
port = 8085
# Server host
host = "localhost"
# Shutdown endpoint
shutdown_endpoint = "/api/shutdown"

[output]
# Base directory for test results (relative to test/ui directory)
# Each test run creates: results/{test-name}-{datetime}/
results_base_dir = "./results"
